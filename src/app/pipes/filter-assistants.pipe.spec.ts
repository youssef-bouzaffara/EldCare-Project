import { FilterAssistantsPipe } from './filter-assistants.pipe';

describe('FilterAssistantsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssistantsPipe();
    expect(pipe).toBeTruthy();
  });
});
