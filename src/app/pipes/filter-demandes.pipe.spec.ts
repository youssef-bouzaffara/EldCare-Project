import { FilterDemandesPipe } from './filter-demandes.pipe';

describe('FilterDemandesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDemandesPipe();
    expect(pipe).toBeTruthy();
  });
});
